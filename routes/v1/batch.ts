import { Router } from 'express';
// import { webhook } from '../../controllers/v1/batch';

const router = Router();

/**
 * 웹훅 로그를 확인하여 처리되지 않은 로그가 있다면 다시 처리
 */
// router.get('/webhook', webhook);

export default router;
